node_modules/
dist/
bin/
release/
src-tauri/target/
src-tauri/gen/
src-tauri/binaries/
.env
*.log
.DS_Store
# ── ▼▼ PRIVATE-BACKUP:BEGIN ▼▼ ───────────────────────────────────────────────
# ここから下のマーカーまでが「秘密ファイル」の定義そのもの。
#   * git 追跡されない = 消えたら復元できない
#   * よって jp225-specs/private-backup/ に控えを置くことを **出荷の条件** にする
# scripts/private-backup.mjs は対象を列挙せず、git check-ignore -v に
# 「どの .gitignore 行が効いたか」を答えさせ、この区間の行だけを秘密と判定する。
# → 秘密ファイルを増やすときは必ずこの区間に書くこと。書けば自動で照合対象になる。
#   (区間外の ignore = ビルド成果物や .env はバックアップ対象にしない)
#
# トレードシグナルの非公開 phase-exit(数値・段階ルールは公開しない)
server/signalTrade/exit/private.ts
# 上の等価性テスト(パラメータ化前の実装を凍結したコピー=実数値を含む)。公開リポには載せない。
server/signalTrade/exit/private.test.ts
# ── ▲▲ PRIVATE-BACKUP:END ▲▲ ─────────────────────────────────────────────────

# ── オフライン再生ハーネス(開発用・公開しない) ───────────────────────────────
# 運用機のローカルパス(書き出しフォルダ・API キーファイルの場所)を含むため、
# 公開リポ(jp225-monitor)へは載せない。実データ(凍結した足/節目/根拠文)も同様。
# ★PRIVATE-BACKUP の区間外に置くこと(バックアップ照合の対象ではない)。
scripts/plan-replay/

# ── 開発の内部手順(スキル)。公開リポには載せない ─────────────────────────────
# ★PRIVATE-BACKUP の区間外に置くこと(バックアップ照合の対象ではない)。
.claude/skills/

# 作業の規範(毎セッション読まれる)。個人的な内容なので公開リポには載せない。
CLAUDE.md

# 作業用の下書き・参考画像(公開リポには載せない)
newss/

# SDD の作業台帳(ローカル限定・git に入れない)
.superpowers/sdd/
