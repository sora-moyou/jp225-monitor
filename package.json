{
  "name": "finance-monitor",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n SRV,WEB -c blue,green \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "tsx watch --env-file=.env server/index.ts",
    "dev:web": "vite",
    "build": "npm run build:web && npm run build:server",
    "build:web": "vite build",
    "build:server": "node scripts/build-server.mjs",
    "package": "npm run build && node scripts/package-sea.mjs",
    "package:pkg": "npm run build && pkg dist/server.cjs --targets node18-win-x64 --output bin/jp225-monitor",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "pkg": {
    "scripts": [],
    "assets": [
      "dist/web/**/*"
    ]
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "express": "^4.19.0",
    "openai": "^4.50.0",
    "rss-parser": "^3.13.0",
    "yahoo-finance2": "^2.11.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^20.14.0",
    "@yao-pkg/pkg": "^6.20.0",
    "concurrently": "^8.2.0",
    "esbuild": "^0.28.0",
    "postject": "^1.0.0-alpha.6",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
